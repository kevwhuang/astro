---
const { domain, handle } = Astro.props;
---

<a href={`https://${domain}.com/${handle}`} target="_blank">{domain}</a>

<style lang="scss">
    a {
        text-transform: uppercase;
    }
</style>
